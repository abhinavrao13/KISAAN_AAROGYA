{
  "name": "kisaan-aarogya",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build"
  }
}
